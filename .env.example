# Environment Variables Example
# Copy this file to .env.local and fill in your actual values
# These variables are used during build time for static generation

# Your site URL (used for canonical URLs and SEO)
NEXT_PUBLIC_SITE_URL=https://oneqid.com

# Strapi CMS API URL (must be publicly accessible during CI/CD builds)
NEXT_PUBLIC_STRAPI_URL=https://api.example.com

# Optional: Strapi authentication token (if your API requires auth)
# STRAPI_API_TOKEN=your_strapi_token_here

# Internal: Used as fallback if NEXT_PUBLIC_STRAPI_URL is not set
# Default: http://localhost:1337 (for local development)
url=http://localhost:1337

# Node.js configuration for builds (optional, set in CI/CD if needed)
# NODE_OPTIONS=--max-old-space-size=4096

# Build environment (automatically set to 'production' in CI/CD)
# NODE_ENV=production

# Disable Next.js telemetry in CI/CD (automatically set)
# NEXT_TELEMETRY_DISABLED=1

# Optional: Enable debug logging during build
# DEBUG=*